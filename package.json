{
  "name": "voting-alyra",
  "version": "1.0.0",
  "description": "A voting application built with Hardhat and OpenZeppelin",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "start": "hardhat node"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CygiK/Voting-systeme-exercice.git"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
